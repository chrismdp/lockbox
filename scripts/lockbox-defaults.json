{
  "tools": {
    "safe": [
      "Glob", "Grep", "Read", "Write", "Edit", "NotebookEdit",
      "WebSearch", "ToolSearch", "Skill",
      "Task", "TaskOutput", "TaskCreate", "TaskGet", "TaskUpdate", "TaskList",
      "TodoWrite",
      "EnterPlanMode", "ExitPlanMode",
      "AskUserQuestion", "EnterWorktree"
    ],
    "acting": [
      "TaskStop",
      "TeamCreate", "TeamDelete", "SendMessage"
    ],
    "unsafe": [],
    "unsafe_acting": [
      "WebFetch"
    ]
  },
  "mcp_tools": {
    "mcp__perplexity__perplexity_search": "safe",
    "mcp__perplexity__perplexity_ask": "unsafe",
    "mcp__perplexity__perplexity_reason": "unsafe",
    "mcp__perplexity__perplexity_research": "unsafe",
    "mcp__nanoclaw__list_tasks": "safe",
    "mcp__nanoclaw__send_message": "acting",
    "mcp__nanoclaw__schedule_task": "acting"
  },
  "mcp_default": "acting",
  "bash_patterns": {
    "override_safe": [
      "--help"
    ],
    "unsafe_acting": [
      "curl\\s",
      "wget\\s"
    ],
    "unsafe": [],
    "acting": [
      "git\\s+(push|reset|rebase)",
      "op\\s",
      "npm\\s+publish",
      "sudo\\s",
      "ssh\\s",
      "scp\\s",
      "nc\\s",
      "netcat\\s",
      "ncat\\s"
    ],
    "safe": [
      "^(date|ls|cat|head|tail|wc|find|grep|rg)(\\s|$)",
      "^(echo|printf|tee|touch|sort|uniq|tr|cut|xargs)(\\s|$)",
      "^(jq|yq|sed|awk|diff|comm|paste)(\\s|$)",
      "^(python3?|node|ruby|perl)(\\s|$)",
      "^(timeout|time|env|which|type|file|stat|readlink)(\\s|$)",
      "^(basename|dirname|realpath|mktemp)(\\s|$)",
      "^(tar|zip|unzip|gzip|gunzip)(\\s|$)",
      "^(sha256sum|md5sum|base64|xxd)(\\s|$)",
      "git\\s+(status|log|diff|show|branch|add|commit|stash|fetch|tag|remote)",
      "^(rm|mv|cp|mkdir)\\s",
      "^claude\\s"
    ]
  }
}
